<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Required for Foreground Service (CallService) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/> <!-- For Android 13+ Notifications -->
    <!-- Required for Microphone -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Required for Keystore -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" /> <!-- If using biometric prompt -->

    <application
        android:name=".EvaApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EvaV3"
        android:usesCleartextTraffic="true" <!-- Added for HTTP traffic during development. Consider removing for production. -->
    tools:targetApi="31">

    <!-- MainActivity -->
    <activity
        android:name=".MainActivity"
        android:exported="true"
        android:theme="@style/Theme.EvaV3">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

    <!-- CallService -->
    <!-- Ensure com.example.evav3.services.CallService exists -->
    <service
        android:name=".services.CallService"
        android:foregroundServiceType="microphone|dataSync"
        android:exported="false" />

    <!-- AppWidgetProvider -->
    <!-- Ensure com.example.evav3.widgets.CallWidgetProvider exists -->
    <!-- Ensure @xml/call_widget_info exists -->
    <receiver
        android:name=".widgets.CallWidgetProvider"
        android:exported="false">
        <intent-filter>
            <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            <action android:name="com.example.evav3.ACTION_WIDGET_TOGGLE_CALL" />
        </intent-filter>
        <meta-data
            android:name="android.appwidget.provider"
            android:resource="@xml/call_widget_info" />
    </receiver>

</application>

    </manifest>